# Database Connection Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/urban_nav
spring.datasource.username=urban
spring.datasource.password=urban
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Flyway Settings (ensure Flyway can access the database)
spring.flyway.url=jdbc:postgresql://localhost:5432/urban_nav
spring.flyway.user=urban
spring.flyway.password=urban

# Debug logging
logging.level.org.springframework.security=DEBUG
logging.level.com.yourcompany=DEBUG
logging.level.org.springframework.web=DEBUG

# Show exact error message in API responses
server.error.include-message=always

# JWT Configuration
app.jwt.secret=ldSBYoljpC53dEWwHXhnViUqFHwNH7Fgn61mTnxfmo5OvKvb4z1IEpqTdDnrwQlxa2lvGQ3WnDQUChuu6gbaVg==
app.jwt.expiration-ms=86400000

# Optional: Hibernate DDL Options
# spring.jpa.hibernate.ddl-auto=none   # use for production to avoid schema changes
# spring.jpa.hibernate.ddl-auto=create-drop  # use for development to drop/recreate schema
