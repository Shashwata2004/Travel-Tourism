## Supabase (Session Pooler)
# Prefer the pooler host+port for teammates without local Postgres.
SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:5432/postgres?sslmode=require
SPRING_DATASOURCE_USERNAME=postgres.<project_ref>
SPRING_DATASOURCE_PASSWORD=your-strong-password

## JWT
APP_JWT_SECRET=replace-with-strong-secret
APP_JWT_EXPIRATION_MS=86400000

## Keep pool sizes tiny on free tiers / session pooler
DB_POOL_SIZE=1

## Flyway startup tolerance for brief pool saturation
SPRING_FLYWAY_CONNECT_RETRIES=10
SPRING_FLYWAY_CONNECT_RETRIES_INTERVAL=3s

## Optional: Transaction Pooler alternative (more headroom, port 6543)
# SPRING_DATASOURCE_URL=jdbc:postgresql://aws-1-ap-southeast-2.pooler.supabase.com:6543/postgres?sslmode=require

## Admin bootstrap (configure only in backend/.env; never commit real secrets)
# If ADMIN_EMAIL is not set, admin seeding is skipped.
# ADMIN_EMAIL=your-admin@example.com
# ADMIN_USERNAME=admin
# ADMIN_PASSWORD=   # set locally in backend/.env
