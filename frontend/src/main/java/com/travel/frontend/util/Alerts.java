/* Placeholder for shared alert helpers; currently unused but kept so new
   pop-up message utilities can live in one simple class later on.
   Leaving the file around also signals where future JavaFX Alert wrappers
   should be added so controllers stay tidy. */
