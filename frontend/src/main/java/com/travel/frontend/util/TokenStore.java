/* Reserved spot for persisting tokens securely (for example in files or
   encrypted storage) if we ever extend beyond the simple in-memory session.
   Acts like a signpost for future offline login or “remember me” work so the
   logic does not end up scattered. */
